Confirma qual arquivo está em jogo antes da conversão.

```jsx
<FileSummary name="apostila-direito-adm.pdf" meta="903 páginas · 108 MB"
  action={<Button variant="ghost" size="sm">Trocar</Button>} />
```

Nome truncado com ellipsis (nunca quebra em duas linhas); metadados em uma linha só.
