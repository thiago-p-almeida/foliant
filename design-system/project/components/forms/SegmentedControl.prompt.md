Escolha única entre 2–4 opções curtas — substitui `select` e campos de código.

```jsx
<SegmentedControl label="Idioma do livro" options={["Português", "Inglês", "Espanhol"]} />
```

Sempre rótulo humano, nunca código de idioma. Para mais de 4 opções, use outro padrão (lista).
